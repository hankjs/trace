# 适配器模式 & 多轮对话支持

课程
Agent OS 个人生产系统实战
适配器模式 & 多轮对话支持
适配器模式 & 多轮对话支持

约 16 分钟

下载本节完整代码

上一轮，飞书里的一句话已经能启动 Claude Code。可你在同一个话题里追问“那刚才提到的 xxx 案呢”，它依然会从头开始。

程序已经拿到了执行引擎的 session_id，却只把它打印在终端里。这一轮，我们会把它存进 Agent OS 会话：同一话题里的追问带上 --resume，新话题则从干净上下文开始。

我们还会把 Claude Code 的参数和事件解析从进程控制里拆出来，为后续接入其他 CLI 留好位置。

进入项目，安装依赖并确认版本能编译：

bash
复制
pnpm install
pnpm build

两个 session id 先解释一下

动手之前，先把这两个 id 分清楚。

session.id 由 Agent OS 生成，负责识别飞书话题和管理执行状态。

session_id 由 Claude Code 返回。再次启动 claude 时传入 --resume <session_id>，就能找回本机保存的上下文。

--continue 只会寻找当前工作目录里最近的一次会话。

恢复时还要使用相同的工作目录。CLAUDE_WORKDIR 中途换了位置，即使 session_id 没变，也可能找不到原记录。

先定义 CLI 适配层

上一版的 claude-runner.ts 同时负责子进程、参数和 JSON 解析。我们把供应商差异在一层抹平。

新建 src/cli/types.ts：

src/cli/types.ts
查看
复制
export type CliId = "claude";

export type CliEvent =
  | { type: "session"; sessionId: string }
  | { type: "result"; answer: string; sessionId?: string }
  | { type: "error"; message: string; sessionId?: string };

export interface CliAdapter {
  readonly id: CliId;
  readonly command: string;
  readonly displayName: string;
  buildArgs(prompt: string): string[];
  buildResumeArgs(prompt: string, sessionId: string): string[];
  parseEvent(line: string): CliEvent | undefined;
}

export interface CliRunResult {
  answer: string;
  sessionId?: string;
}


这个接口把差异压缩成三个动作。

新会话怎样组装参数，由 buildArgs() 决定。
已有会话怎样续接，由 buildResumeArgs() 决定。
原始输出怎样变成统一事件，由 parseEvent() 决定。

进程启动、超时和取消留在通用运行器里；过程事件下一节再处理。

保存文件后运行：

bash
复制
pnpm start


现有链路仍会正常启动。保持终端开着，后面的文件保存后会自动重新加载。

把 Claude Code 放进适配器

新建 src/cli/claude-adapter.ts：

src/cli/claude-adapter.ts
查看
复制
import type { CliAdapter, CliEvent } from "./types.js";

interface ClaudeEvent {
  type?: unknown;
  subtype?: unknown;
  is_error?: unknown;
  result?: unknown;
  session_id?: unknown;
}

function outputArgs(prompt: string): string[] {
  return ["-p", prompt, "--output-format", "stream-json", "--verbose"];
}

export class ClaudeAdapter implements CliAdapter {
  readonly id = "claude" as const;
  readonly command = "claude";
  readonly displayName = "Claude Code";

  buildArgs(prompt: string): string[] {
    return outputArgs(prompt);
  }

  buildResumeArgs(prompt: string, sessionId: string): string[] {
    return ["--resume", sessionId, ...outputArgs(prompt)];
  }

  parseEvent(line: string): CliEvent | undefined {
    let event: ClaudeEvent;
    try {
      event = JSON.parse(line) as ClaudeEvent;
    } catch {
      return undefined;
    }

    const sessionId =
      typeof event.session_id === "string" ? event.session_id : undefined;

    if (event.type === "system" && event.subtype === "init" && sessionId) {
      return { type: "session", sessionId };
    }

    if (event.type !== "result") return undefined;

    if (event.is_error) {
      return {
        type: "error",
        message:
          typeof event.result === "string"
            ? event.result
            : "Claude Code 执行失败",
        ...(sessionId ? { sessionId } : {}),
      };
    }

    if (typeof event.result !== "string") return undefined;

    return {
      type: "result",
      answer: event.result,
      ...(sessionId ? { sessionId } : {}),
    };
  }
}


Claude Code 启动后会发出 system/init，任务结束时还会发出 result。适配器从两处捕获 session_id，再翻译成统一事件。

第一轮参数由 buildArgs() 生成，后续追问改用 buildResumeArgs()，加入 --resume <session_id>。入口服务始终调用同一套接口。

通用 Runner 开发

接下来把 claude-runner.ts 改造成通用 Runner。

新建 src/cli/runner.ts：

src/cli/runner.ts
查看
复制
import { spawn } from "node:child_process";
import { createInterface } from "node:readline";
import type { CliAdapter, CliRunResult } from "./types.js";

const DEFAULT_TIMEOUT_MS = 10 * 60 * 1000;

export interface RunCliOptions {
  adapter: CliAdapter;
  prompt: string;
  cwd: string;
  sessionId?: string;
  signal?: AbortSignal;
  timeoutMs?: number;
}

export function runCli(options: RunCliOptions): Promise<CliRunResult> {
  const {
    adapter,
    prompt,
    cwd,
    sessionId,
    signal,
    timeoutMs = DEFAULT_TIMEOUT_MS,
  } = options;

  const args = sessionId
    ? adapter.buildResumeArgs(prompt, sessionId)
    : adapter.buildArgs(prompt);

  return new Promise((resolve, reject) => {
    const child = spawn(adapter.command, args, {
      cwd,
      signal,
      stdio: ["ignore", "pipe", "pipe"],
    });
    const lines = createInterface({ input: child.stdout });
    let observedSessionId = sessionId;
    let finalResult: CliRunResult | undefined;
    let resultError: Error | undefined;
    let stderr = "";
    let settled = false;
    let timedOut = false;

    const timer = setTimeout(() => {
      timedOut = true;
      child.kill("SIGTERM");
    }, timeoutMs);

    const finish = () => clearTimeout(timer);
    const fail = (error: Error) => {
      if (settled) return;
      settled = true;
      finish();
      reject(error);
    };

    lines.on("line", (line) => {
      const event = adapter.parseEvent(line);
      if (!event) return;

      if (event.sessionId) observedSessionId = event.sessionId;

      if (event.type === "error") {
        resultError = new Error(event.message);
        return;
      }

      if (event.type === "result") {
        finalResult = {
          answer: event.answer,
          sessionId: event.sessionId ?? observedSessionId,
        };
      }
    });

    child.stderr.on("data", (chunk: Buffer | string) => {
      stderr += chunk.toString();
    });

    child.once("error", (error) => {
      if (timedOut) return fail(new Error(`${adapter.displayName} 执行超时`));
      if (signal?.aborted) {
        return fail(new Error(`${adapter.displayName} 执行已取消`));
      }
      fail(error);
    });

    child.once("close", (code) => {
      if (settled) return;
      if (timedOut) return fail(new Error(`${adapter.displayName} 执行超时`));
      if (signal?.aborted) {
        return fail(new Error(`${adapter.displayName} 执行已取消`));
      }
      if (resultError) return fail(resultError);
      if (code !== 0) {
        return fail(
          new Error(
            stderr.trim() || `${adapter.displayName} 退出，状态码 ${code}`,
          ),
        );
      }
      if (!finalResult) {
        return fail(new Error(`${adapter.displayName} 没有返回最终结果`));
      }

      settled = true;
      finish();
      resolve(finalResult);
    });
  });
}


这段代码保留了逐行读取、取消和错误回传，并补上十分钟超时。超时后运行器发送 SIGTERM，卡片会显示“执行超时”。

observedSessionId 记住最近一次出现的会话标识。即使最终 result 没有携带它，Runner 仍能返回可持久化的 sessionId。

Runner 只读取适配器翻译后的 CliEvent，不再关心供应商的原生字段。

Session 数据层支持 cliSessionId

打开 src/core/session-manager.ts，让 CliId 成为会话模型的一部分，并增加可选的 cliSessionId：

src/core/session-manager.ts
查看
复制
import type { CliId } from "../cli/types.js";

export interface Session {
  id: string;
  threadId: string;
  chatId: string;
  cliId: CliId;
  cliSessionId?: string;
  status: SessionStatus;
  createdAt: string;
  updatedAt: string;
}


字段必须可选，因为旧的 data/sessions.json 里还没有它。第一次任务结束后，程序才会拿到这个值。

在 SessionManager 里加入保存方法：

src/core/session-manager.ts
查看
复制
async setCliSessionId(
  sessionId: string,
  cliSessionId: string,
): Promise<Session> {
  const current = this.get(sessionId);
  if (!current) throw new Error(`会话不存在: ${sessionId}`);
  if (!cliSessionId) throw new Error("CLI 会话 ID 不能为空");

  const updated: Session = {
    ...current,
    cliSessionId,
    updatedAt: this.now().toISOString(),
  };
  const key = sessionKey(updated.chatId, updated.threadId);
  this.sessions.set(key, updated);

  try {
    await this.persist();
  } catch (error) {
    if (this.sessions.get(key) === updated) this.sessions.set(key, current);
    throw error;
  }

  return updated;
}


保存失败时回滚内存状态，避免磁盘没有 cliSessionId、内存却以为已经保存。

再打开 src/core/session-store.ts，在 SessionSchema 里加入同名字段：

src/core/session-store.ts
查看
复制
const SessionSchema = z.object({
  id: z.string().min(1),
  threadId: z.string().min(1),
  chatId: z.string().min(1),
  cliId: z.literal("claude"),
  cliSessionId: z.string().min(1).optional(),
  status: z.enum(["creating", "active", "idle", "closed"]),
  createdAt: z.iso.datetime(),
  updatedAt: z.iso.datetime(),
});


保存后观察原来的终端。项目应该重新启动并恢复已有会话。

在入口处完成两层映射

现在可以删掉旧的一次性运行模块：

bash
复制
rm src/cli/claude-runner.*


打开 src/index.ts，替换旧运行器的导入，并创建 Claude Code 适配器：

src/index.ts
查看
复制
import { ClaudeAdapter } from "./cli/claude-adapter.js";
import { runCli } from "./cli/runner.js";

const cliWorkdir = resolve(process.env.CLAUDE_WORKDIR ?? process.cwd());
const cliAdapter = new ClaudeAdapter();


把 executeCli() 改成接收已有会话标识：

src/index.ts
查看
复制
function executeCli(
  prompt: string,
  sessionId: string | undefined,
  signal: AbortSignal,
) {
  return runCli({
    adapter: cliAdapter,
    prompt,
    cwd: cliWorkdir,
    sessionId,
    signal,
  });
}


往上找到两行启动日志，把第二行改成读取 cliAdapter.command：

src/index.ts
查看
复制
console.log("Agent OS 启动，正在建立飞书长连接…");
console.log(`[CLI] command=${cliAdapter.command} cwd=${cliWorkdir}`);


再找到 formatSessionStatus()，用下面这份完整代码替换原函数。新增的是“CLI 会话”这一行：

src/index.ts
查看
复制
function formatSessionStatus(session: Session): string {
  return [
    `会话：${session.id}`,
    `状态：${STATUS_LABELS[session.status]}`,
    `执行引擎：${session.cliId}`,
    `CLI 会话：${session.cliSessionId ?? "(尚未建立)"}`,
    `话题：${session.threadId}`,
    `更新时间：${session.updatedAt}`,
  ].join("\n");
}


真正的续接发生在调用点。用下面这段替换原来的 executeCli(resolved, run.signal)：

注意，后面的 .catch()、.finally() 和最外层 .catch() 保持原样，只需要改 executeCli 入参和 then 里面的内容。

src/index.ts
查看
复制
void executeCli(resolved, session.cliSessionId, run.signal).then(
  async (result) => {
    if (result.sessionId && result.sessionId !== session.cliSessionId) {
      await sessions.setCliSessionId(session.id, result.sessionId);
    }

    await bot.updateCard(
      cardId,
      buildTaskCard({
        title: "Claude Code 任务",
        status: "success",
        progress: 100,
        detail: "执行完成",
      }),
    );
    await bot.reply(msg.messageId, result.answer, hasThread);
    console.log(`[CLI] 完成 session_id=${result.sessionId ?? "(无)"}`);
  },
);


入口只看 cliSessionId：没有值就新建，有值就续接原来的会话。

让飞书代码块也能成为提示词

有时候我们会把日志或代码粘成飞书代码块。这类消息在 post 富文本中，标签是 code_block。旧解析器只取 text，会把提示词变成空字符串。

打开 src/im/lark.ts，用下面的元素类型与渲染函数替换旧的 extractText()：

src/im/lark.ts
查看
复制
interface PostElement {
  tag?: string;
  text?: string;
  user_id?: string;
}

function renderPostElement(element: PostElement): string {
  if (element.tag === "at") return element.user_id ?? "";
  if (element.tag === "br") return "\n";
  if (["text", "a", "code", "code_block", "md"].includes(element.tag ?? "")) {
    return element.text ?? "";
  }
  return "";
}

export function extractMessageText(
  messageType: string,
  content: string,
): string {
  const parsed = JSON.parse(content);

  if (messageType === "text") {
    return parsed.text ?? "";
  }

  if (messageType === "post") {
    const paragraphs: PostElement[][] = parsed.content ?? [];
    return paragraphs
      .map((paragraph) => paragraph.map(renderPostElement).join(""))
      .filter(Boolean)
      .join("\n")
      .trim();
  }

  return "";
}


在事件转换处，把原来的函数名同步改掉：

src/im/lark.ts
查看
复制
text: extractMessageText(m.message_type, m.content),


现在这些富文本内容都会保留，@ 占位符仍交给后面的提及还原逻辑。

在同一个话题里验收记忆

保存全部文件，确认终端已经自动重启。也可以单独运行一次类型检查：

bash
复制
pnpm build


打开一个新的飞书话题，发送：

text
复制
@MyBot 请记住暗号“Agent 操作系统”，只回复“记住了”


等它完成后，留在同一个话题里追问：

text
复制
@MyBot 我刚才让你记住的暗号是什么？


回答里应该出现“Agent 操作系统”。接着发送 /status，结果中应该同时出现 Agent OS 的“会话”和 Claude Code 的“CLI 会话”。

再开一个话题问同样的问题。它不应该记得刚才的暗号，这个对照同时验证了Session 的还原与隔离。

打开 data/sessions.json，对应记录中也应该多出 cliSessionId。它只是恢复指针，完整对话仍由 Claude Code 保存在本机。

现在，飞书话题终于拥有了连续记忆。你可以在手机上说“继续”“按刚才的方案改”，执行引擎知道你在接着哪段工作。

不过，任务执行期间卡片还是略显生硬，没有流式效果。接下来我们会消费 stream-json 的过程事件，让文件读取、工具调用和执行进度逐步回到同一张卡片上。

有了稳定的会话，接下来就该让它的工作过程真正被看见了。我们下一节精彩继续👋

参考资料
Claude Code：Manage sessions
Claude Code：CLI reference
Claude Code：Run Claude Code programmatically
上一篇
全链路首通：飞书提问，Claude Code 回答
下一篇 · 第四章：接入 CLI 引擎
看见 Claude Code 的每一步：打通流式事件链
编辑器


---
## 代码块


```bash
pnpm install
pnpm build
```


```typescript
export type CliId = "claude";

export type CliEvent =
  | { type: "session"; sessionId: string }
  | { type: "result"; answer: string; sessionId?: string }
  | { type: "error"; message: string; sessionId?: string };

export interface CliAdapter {
  readonly id: CliId;
  readonly command: string;
  readonly displayName: string;
  buildArgs(prompt: string): string[];
  buildResumeArgs(prompt: string, sessionId: string): string[];
  parseEvent(line: string): CliEvent | undefined;
}

export interface CliRunResult {
  answer: string;
  sessionId?: string;
}
```


```bash
pnpm start
```


```typescript
import type { CliAdapter, CliEvent } from "./types.js";

interface ClaudeEvent {
  type?: unknown;
  subtype?: unknown;
  is_error?: unknown;
  result?: unknown;
  session_id?: unknown;
}

function outputArgs(prompt: string): string[] {
  return ["-p", prompt, "--output-format", "stream-json", "--verbose"];
}

export class ClaudeAdapter implements CliAdapter {
  readonly id = "claude" as const;
  readonly command = "claude";
  readonly displayName = "Claude Code";

  buildArgs(prompt: string): string[] {
    return outputArgs(prompt);
  }

  buildResumeArgs(prompt: string, sessionId: string): string[] {
    return ["--resume", sessionId, ...outputArgs(prompt)];
  }

  parseEvent(line: string): CliEvent | undefined {
    let event: ClaudeEvent;
    try {
      event = JSON.parse(line) as ClaudeEvent;
    } catch {
      return undefined;
    }

    const sessionId =
      typeof event.session_id === "string" ? event.session_id : undefined;

    if (event.type === "system" && event.subtype === "init" && sessionId) {
      return { type: "session", sessionId };
    }

    if (event.type !== "result") return undefined;

    if (event.is_error) {
      return {
        type: "error",
        message:
          typeof event.result === "string"
            ? event.result
            : "Claude Code 执行失败",
        ...(sessionId ? { sessionId } : {}),
      };
    }

    if (typeof event.result !== "string") return undefined;

    return {
      type: "result",
      answer: event.result,
      ...(sessionId ? { sessionId } : {}),
    };
  }
}
```


```typescript
import { spawn } from "node:child_process";
import { createInterface } from "node:readline";
import type { CliAdapter, CliRunResult } from "./types.js";

const DEFAULT_TIMEOUT_MS = 10 * 60 * 1000;

export interface RunCliOptions {
  adapter: CliAdapter;
  prompt: string;
  cwd: string;
  sessionId?: string;
  signal?: AbortSignal;
  timeoutMs?: number;
}

export function runCli(options: RunCliOptions): Promise<CliRunResult> {
  const {
    adapter,
    prompt,
    cwd,
    sessionId,
    signal,
    timeoutMs = DEFAULT_TIMEOUT_MS,
  } = options;

  const args = sessionId
    ? adapter.buildResumeArgs(prompt, sessionId)
    : adapter.buildArgs(prompt);

  return new Promise((resolve, reject) => {
    const child = spawn(adapter.command, args, {
      cwd,
      signal,
      stdio: ["ignore", "pipe", "pipe"],
    });
    const lines = createInterface({ input: child.stdout });
    let observedSessionId = sessionId;
    let finalResult: CliRunResult | undefined;
    let resultError: Error | undefined;
    let stderr = "";
    let settled = false;
    let timedOut = false;

    const timer = setTimeout(() => {
      timedOut = true;
      child.kill("SIGTERM");
    }, timeoutMs);

    const finish = () => clearTimeout(timer);
    const fail = (error: Error) => {
      if (settled) return;
      settled = true;
      finish();
      reject(error);
    };

    lines.on("line", (line) => {
      const event = adapter.parseEvent(line);
      if (!event) return;

      if (event.sessionId) observedSessionId = event.sessionId;

      if (event.type === "error") {
        resultError = new Error(event.message);
        return;
      }

      if (event.type === "result") {
        finalResult = {
          answer: event.answer,
          sessionId: event.sessionId ?? observedSessionId,
        };
      }
    });

    child.stderr.on("data", (chunk: Buffer | string) => {
      stderr += chunk.toString();
    });

    child.once("error", (error) => {
      if (timedOut) return fail(new Error(`${adapter.displayName} 执行超时`));
      if (signal?.aborted) {
        return fail(new Error(`${adapter.displayName} 执行已取消`));
      }
      fail(error);
    });

    child.once("close", (code) => {
      if (settled) return;
      if (timedOut) return fail(new Error(`${adapter.displayName} 执行超时`));
      if (signal?.aborted) {
        return fail(new Error(`${adapter.displayName} 执行已取消`));
      }
      if (resultError) return fail(resultError);
      if (code !== 0) {
        return fail(
          new Error(
            stderr.trim() || `${adapter.displayName} 退出，状态码 ${code}`,
          ),
        );
      }
      if (!finalResult) {
        return fail(new Error(`${adapter.displayName} 没有返回最终结果`));
      }

      settled = true;
      finish();
      resolve(finalResult);
    });
  });
}
```


```typescript
import type { CliId } from "../cli/types.js";

export interface Session {
  id: string;
  threadId: string;
  chatId: string;
  cliId: CliId;
  cliSessionId?: string;
  status: SessionStatus;
  createdAt: string;
  updatedAt: string;
}
```


```typescript
async setCliSessionId(
  sessionId: string,
  cliSessionId: string,
): Promise<Session> {
  const current = this.get(sessionId);
  if (!current) throw new Error(`会话不存在: ${sessionId}`);
  if (!cliSessionId) throw new Error("CLI 会话 ID 不能为空");

  const updated: Session = {
    ...current,
    cliSessionId,
    updatedAt: this.now().toISOString(),
  };
  const key = sessionKey(updated.chatId, updated.threadId);
  this.sessions.set(key, updated);

  try {
    await this.persist();
  } catch (error) {
    if (this.sessions.get(key) === updated) this.sessions.set(key, current);
    throw error;
  }

  return updated;
}
```


```typescript
const SessionSchema = z.object({
  id: z.string().min(1),
  threadId: z.string().min(1),
  chatId: z.string().min(1),
  cliId: z.literal("claude"),
  cliSessionId: z.string().min(1).optional(),
  status: z.enum(["creating", "active", "idle", "closed"]),
  createdAt: z.iso.datetime(),
  updatedAt: z.iso.datetime(),
});
```


```bash
rm src/cli/claude-runner.*
```


```typescript
import { ClaudeAdapter } from "./cli/claude-adapter.js";
import { runCli } from "./cli/runner.js";

const cliWorkdir = resolve(process.env.CLAUDE_WORKDIR ?? process.cwd());
const cliAdapter = new ClaudeAdapter();
```


```typescript
function executeCli(
  prompt: string,
  sessionId: string | undefined,
  signal: AbortSignal,
) {
  return runCli({
    adapter: cliAdapter,
    prompt,
    cwd: cliWorkdir,
    sessionId,
    signal,
  });
}
```


```typescript
console.log("Agent OS 启动，正在建立飞书长连接…");
console.log(`[CLI] command=${cliAdapter.command} cwd=${cliWorkdir}`);
```


```typescript
function formatSessionStatus(session: Session): string {
  return [
    `会话：${session.id}`,
    `状态：${STATUS_LABELS[session.status]}`,
    `执行引擎：${session.cliId}`,
    `CLI 会话：${session.cliSessionId ?? "(尚未建立)"}`,
    `话题：${session.threadId}`,
    `更新时间：${session.updatedAt}`,
  ].join("\n");
}
```


```typescript
void executeCli(resolved, session.cliSessionId, run.signal).then(
  async (result) => {
    if (result.sessionId && result.sessionId !== session.cliSessionId) {
      await sessions.setCliSessionId(session.id, result.sessionId);
    }

    await bot.updateCard(
      cardId,
      buildTaskCard({
        title: "Claude Code 任务",
        status: "success",
        progress: 100,
        detail: "执行完成",
      }),
    );
    await bot.reply(msg.messageId, result.answer, hasThread);
    console.log(`[CLI] 完成 session_id=${result.sessionId ?? "(无)"}`);
  },
);
```


```typescript
interface PostElement {
  tag?: string;
  text?: string;
  user_id?: string;
}

function renderPostElement(element: PostElement): string {
  if (element.tag === "at") return element.user_id ?? "";
  if (element.tag === "br") return "\n";
  if (["text", "a", "code", "code_block", "md"].includes(element.tag ?? "")) {
    return element.text ?? "";
  }
  return "";
}

export function extractMessageText(
  messageType: string,
  content: string,
): string {
  const parsed = JSON.parse(content);

  if (messageType === "text") {
    return parsed.text ?? "";
  }

  if (messageType === "post") {
    const paragraphs: PostElement[][] = parsed.content ?? [];
    return paragraphs
      .map((paragraph) => paragraph.map(renderPostElement).join(""))
      .filter(Boolean)
      .join("\n")
      .trim();
  }

  return "";
}
```


```typescript
text: extractMessageText(m.message_type, m.content),
```


```bash
pnpm build
```


```text
@MyBot 请记住暗号“Agent 操作系统”，只回复“记住了”
```


```text
@MyBot 我刚才让你记住的暗号是什么？
```
